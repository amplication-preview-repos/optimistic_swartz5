/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GrpcMethod } from "@nestjs/microservices";
import { AlbumService } from "../album.service";
import { AlbumCreateInput } from "./AlbumCreateInput";
import { AlbumWhereInput } from "./AlbumWhereInput";
import { AlbumWhereUniqueInput } from "./AlbumWhereUniqueInput";
import { AlbumFindManyArgs } from "./AlbumFindManyArgs";
import { AlbumUpdateInput } from "./AlbumUpdateInput";
import { Album } from "./Album";
import { SongFindManyArgs } from "../../song/base/SongFindManyArgs";
import { Song } from "../../song/base/Song";
import { SongWhereUniqueInput } from "../../song/base/SongWhereUniqueInput";

export class AlbumGrpcControllerBase {
  constructor(protected readonly service: AlbumService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Album })
  @GrpcMethod("AlbumService", "createAlbum")
  async createAlbum(@common.Body() data: AlbumCreateInput): Promise<Album> {
    return await this.service.createAlbum({
      data: {
        ...data,

        artist: data.artist
          ? {
              connect: data.artist,
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        title: true,
        releaseDate: true,
        genre: true,

        artist: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Album] })
  @ApiNestedQuery(AlbumFindManyArgs)
  @GrpcMethod("AlbumService", "albums")
  async albums(@common.Req() request: Request): Promise<Album[]> {
    const args = plainToClass(AlbumFindManyArgs, request.query);
    return this.service.albums({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        title: true,
        releaseDate: true,
        genre: true,

        artist: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Album })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("AlbumService", "album")
  async album(
    @common.Param() params: AlbumWhereUniqueInput
  ): Promise<Album | null> {
    const result = await this.service.album({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        title: true,
        releaseDate: true,
        genre: true,

        artist: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Album })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("AlbumService", "updateAlbum")
  async updateAlbum(
    @common.Param() params: AlbumWhereUniqueInput,
    @common.Body() data: AlbumUpdateInput
  ): Promise<Album | null> {
    try {
      return await this.service.updateAlbum({
        where: params,
        data: {
          ...data,

          artist: data.artist
            ? {
                connect: data.artist,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          title: true,
          releaseDate: true,
          genre: true,

          artist: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Album })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("AlbumService", "deleteAlbum")
  async deleteAlbum(
    @common.Param() params: AlbumWhereUniqueInput
  ): Promise<Album | null> {
    try {
      return await this.service.deleteAlbum({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          title: true,
          releaseDate: true,
          genre: true,

          artist: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/songs")
  @ApiNestedQuery(SongFindManyArgs)
  @GrpcMethod("AlbumService", "findManySongs")
  async findManySongs(
    @common.Req() request: Request,
    @common.Param() params: AlbumWhereUniqueInput
  ): Promise<Song[]> {
    const query = plainToClass(SongFindManyArgs, request.query);
    const results = await this.service.findSongs(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        title: true,
        releaseDate: true,
        duration: true,

        album: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/songs")
  @GrpcMethod("AlbumService", "connectSongs")
  async connectSongs(
    @common.Param() params: AlbumWhereUniqueInput,
    @common.Body() body: SongWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      songs: {
        connect: body,
      },
    };
    await this.service.updateAlbum({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/songs")
  @GrpcMethod("AlbumService", "updateSongs")
  async updateSongs(
    @common.Param() params: AlbumWhereUniqueInput,
    @common.Body() body: SongWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      songs: {
        set: body,
      },
    };
    await this.service.updateAlbum({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/songs")
  @GrpcMethod("AlbumService", "disconnectSongs")
  async disconnectSongs(
    @common.Param() params: AlbumWhereUniqueInput,
    @common.Body() body: SongWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      songs: {
        disconnect: body,
      },
    };
    await this.service.updateAlbum({
      where: params,
      data,
      select: { id: true },
    });
  }
}
